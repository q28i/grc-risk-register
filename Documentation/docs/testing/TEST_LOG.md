# Test Log: Systematic Verification Record

This document records the automated and manual verification cycles conducted across every subsystem of the GRC Risk Register project.

---

| Test ID | Component | Test Performed | Expected Behavior | Actual Behavior | Result | Fix Applied |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **TST-001** | Risk Engine | Cartesian boundary test: all 9 pairs of $(L, I)$ for $L \in \{1,2,3\}$ and $I \in \{1,2,3\}$. | Score $= L \times I$; Level strictly classified (1-2: Low, 3-4: Medium, 6-9: High). | Exact scores and levels returned matching qualitative matrix. | **PASSED** | None required (deterministic formula). |
| **TST-002** | Risk Engine | Invalid input boundary test ($L=0$, $L=4$, $I=-1$, non-integer). | System raises validation error and rejects computation. | HTTP 400 validation error raised. | **PASSED** | Added input bounds guard in `risk_calculator.py`. |
| **TST-003** | Authentication | Valid login with pre-seeded `admin` and `analyst` accounts. | Generates secure session token with correct user role and username. | Session token generated, authenticated user context stored. | **PASSED** | None. |
| **TST-004** | Authentication | Invalid login attempt with bad password. | Rejects with HTTP 401 Unauthorized; no session issued. | Returns HTTP 401 with descriptive error message. | **PASSED** | None. |
| **TST-005** | RBAC Security | Risk User (`analyst`) attempting administrative operations (e.g. user creation, purging). | Rejects request with HTTP 403 Forbidden. | Server denies mutation and returns 403 Forbidden. | **PASSED** | Enforced server-side `@require_role('admin')` decorator. |
| **TST-006** | Asset Inventory | Create new asset with valid data (`Data`, `System`, `Device`, `Service`). | Generates auto-increment ID (`AST-xxx`), persists in SQLite, returns asset object. | Asset created and ID incremented correctly. | **PASSED** | None. |
| **TST-007** | Asset Inventory | Create asset with missing name or invalid type. | Rejects with HTTP 400 Bad Request. | Validation error returned; database untouched. | **PASSED** | Added model schema validation. |
| **TST-008** | Asset Archiving | Soft-delete asset (set `active=False`). | Asset removed from active register; existing linked risks remain intact. | Asset archived safely; referential integrity preserved. | **PASSED** | Implemented soft-delete flag in SQLite queries. |
| **TST-009** | Risk Register | Create new risk linked to valid Asset ID. | Auto-generates `RSK-xxx`, computes score and level, stores status `Open`. | Risk created, score calculated accurately, linked to asset. | **PASSED** | None. |
| **TST-010** | Risk Register | Attempt to create risk linked to non-existent Asset ID. | Rejects with HTTP 400 / 404 referencing invalid foreign key. | Foreign key check fails with HTTP 400 error. | **PASSED** | Added foreign key validation in `database.py`. |
| **TST-011** | Risk Register | Transition risk status through lifecycle (`Open` $\rightarrow$ `In Progress` $\rightarrow$ `Treated` $\rightarrow$ `Closed`). | Updates status and persists remediation notes. | State transition successful; audit log recorded. | **PASSED** | None. |
| **TST-012** | Dashboard KPIs | Aggregate summary counts on empty database and populated database. | Accurately computes Total Assets, Total Risks, Open Risks, High Risks without division by zero. | Returns accurate counts (0 on empty DB, exact sums on populated DB). | **PASSED** | Added zero-safe guards in aggregation queries. |
| **TST-013** | Heatmap Matrix | Aggregate risk counts per $3\times3$ cell. | Sum of all 9 cell counts equals total active risks. | Sum matches total active risks identically. | **PASSED** | Verified grid matrix aggregation in `database.py`. |
| **TST-014** | Reporting | Generate printable risk register report and CSV export. | Table formats cleanly with all metadata columns; CSV contains header and all rows. | Print stylesheet applies cleanly; CSV downloads with exact dataset. | **PASSED** | Added print media CSS rules. |
| **TST-015** | Rebranding Audit | Search entire application codebase for legacy vendor keywords or unapproved staging terms. | Zero occurrences of legacy repo names or staging terms in UI. | 100% clean across all HTML, CSS, JS, and server responses. | **PASSED** | Cleaned template headers and meta tags. |
| **TST-016** | Flat Material UI System | Verify flat Material visual styling tokens, 2px radius, rectangular controls, zero pill shapes, zero glowing gradients. | UI renders in flat Material light mode (#F5F5F5) with purple accent (#6200EE) and clean contrast. | 100% compliant with flat Material guidelines. | **PASSED** | Redesigned `style.css` and `index.html`. |
| **TST-017** | Feature Controls & Demo UX | Verify `demo` account (`demo`/`demo123`), SQLite `feature_controls` persistence, Admin UI toggles, true unauthenticated login screen, 403 backend guards, and refresh spinner feedback. | Demo user can inspect dashboard/assets/risks with disabled actions; backend rejects mutations with 403; Admin can toggle permissions dynamically. | All 31 automated tests passed; live server integration checks passed. | **PASSED** | Built `feature_controls` table, RBAC mutation guards, unauthenticated view, and `test_feature_controls.py`. |
| **TST-018** | Granular Controls & Loading UX | Verify complete removal of quick-sign in credentials shortcuts, 29 granular feature keys across 6 categories in SQLite, real data loading state transitions for dashboard and async tables, dynamic admin toggles, and backend 403 guards. | Login page free of exposed credentials; 29 feature keys categorized; dashboard metrics and tables transition into loading placeholders during fetch; demo mutations return 403; admin retains full access. | All 32 automated tests passed (`run_tests.py`); all 7/7 live verification checks passed (`verify_chg0009.py`). | **PASSED** | Cleaned `index.html`, added 29 controls in `database.py`, added loading states in `dashboard.js`, `reports.js`, `style.css`. |
| **TST-019** | Neutral Development Notices & Language Audit | Verify addition of `#dev-notice-banner`, removal of all account-specific restriction phrases across UI tooltips/messages/toasts, neutral 403 backend error text, and preservation of real data integrity. | Authenticated UI displays unobtrusive development notice; all disabled controls use neutral development tooltips; 0 account-restriction phrases found in source; backend enforces 403 with neutral error message. | All 32 automated tests passed (`run_tests.py`); all 5/5 live verification checks passed (`verify_chg0010.py`). | **PASSED** | Added `#dev-notice-banner` in `index.html` & `style.css`, cleaned language in `app.js`, `dashboard.js`, `reports.js`, `app.py`. |
| **TST-020** | Plug-and-Play Launcher & Release Packaging | Verify double-click launcher execution (`Start GRC Risk Register.bat` / `.exe`), background server detachment, HTTP readiness polling, duplicate server detection, clean clone DB auto-creation, minimal public README, and Git exclusions. | Launcher starts server, polls port 8000, opens default browser, and exits while server continues running; second launch detects active server without duplicate port binding; fresh clone auto-initializes SQLite database. | All 32 automated tests passed (`run_tests.py`); clean clone simulation passed; cold start & duplicate detection verified. | **PASSED** | Built `Start GRC Risk Register.bat`, `Start GRC Risk Register.exe`, `Launcher.cs`, `README.md`, `README_ADMIN.md`, `.gitignore`, `LICENSE`. |
